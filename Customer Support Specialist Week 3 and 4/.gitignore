# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/
.venv/

# Raw data (large source CSVs; regenerate data/processed via data_prep.py)
data/raw/

# Full generated batch output (12MB; regenerate via `python -m src.batch_classify`).
# A small committed sample (classified_tickets.sample.json) drives the demo UI.
data/processed/classified_tickets.json

# Runtime UI artifact (agent override decisions logged during a session)
results/override_log.csv

# OS
.DS_Store
.gstack/
